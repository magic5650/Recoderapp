<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:toolbar="http://schemas.android.com/apk/res-auto"
    android:id="@+id/root_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:divider="@drawable/shapeline"
    android:showDividers="middle"
    tools:context="com.magicrecoder.recoderapp.RecorderActivity">
    <android.support.v7.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:minHeight="?attr/actionBarSize"
        android:background="?attr/colorPrimary"
        android:fitsSystemWindows="true"
        android:clipToPadding="false"
        toolbar:popupTheme="@style/ToolbarPopupTheme"
        toolbar:logo="@mipmap/ic_launcher"
        toolbar:navigationIcon="@drawable/ic_menu_white_24dp"
        toolbar:subtitle="录音机"
        toolbar:subtitleTextColor="@color/color_white"
        toolbar:title="诗宁"
        toolbar:titleTextColor="@color/color_white">
    </android.support.v7.widget.Toolbar>
    <ListView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:id="@+id/recorder_ListView"
        android:layout_gravity="center_horizontal|start"
        android:divider="@drawable/shapeline"
        android:layout_weight="3"
        android:drawSelectorOnTop="true" />
    <Chronometer
        android:id="@+id/chronometer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:gravity="center"
        android:textColor="@color/abc_secondary_text_material_light"
        android:textSize="70dp"
        android:layout_weight="2" />
    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:orientation="horizontal">
        <ImageView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:id="@+id/save_record"
            android:layout_gravity="center_horizontal"
            android:padding="25dp" />
        <ImageView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:id="@+id/begin_record"
            android:layout_gravity="center_horizontal"
            android:src="@mipmap/ic_toggle_radio_button_on"
            android:padding="10dp" />
        <ImageView
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:id="@+id/Broadcast_record"
            android:layout_gravity="center_horizontal" />
    </LinearLayout>
</LinearLayout>

